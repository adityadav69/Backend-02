<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create new Chat</title>
    <style>
        form {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        input {
            width: 200px;
            height: 40px;
        }
        button{
            width: 100px;
            height: 40px;
            background: rgb(1, 1, 41);
            color: white;
            border-style: none;
            border-radius: 4%;
            cursor: pointer;
        }
        textarea{
            width: 25vw;
        }
    </style>
</head>

<body>
    <h1>Edit message</h1>
    <div class="form">
        <form method="post" action="/chats/<%=chat._id%>?_method=PUT">
            <input type="text" name="from" disabled value="<%=chat.from%>">
            <textarea name="newmsg" rows="5" cols="10"><%=chat.msg%></textarea>
            <input type="text" name="to" disabled value="<%=chat.to%>">
            <button>Edit Chat</button>
        </form>
    </div>
</body>

</html>