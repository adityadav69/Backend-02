<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
     <style>
        *{
            margin: 0;
            padding: 0;
        }

        body{
            background: #001313;
            color: white;
            font-family: Arial, Helvetica, sans-serif;
            font-size: 20px;
            width: 90vw;
            height: 90vh;
            margin:auto auto;
        }
        .container{
            width: 80vw;
            height: 80vh;
            padding: 10px;
            display: flex;
            flex-direction: column;
            border: 2px solid salmon;
            background: #22011a;
        }

        input{
            width: 300px;
            height: 40px;
            font-size: 20px;
        }
        form{
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        button{
            background: rgb(5, 5, 174);
            border: none;
            color: white;
            border-radius: 10px;
            width: 100px;
            height: 50px;
            font-weight: 700;
            font-size: 20px;
            cursor: pointer;
        }

    </style>
</head>
<body>
    <div class="container">
    <form method="post" action="http://localhost:3000/users/find">
        <input type="text" name="userName" required placeholder="Enter userName">
        <input type="text" name="password" required placeholder="Enter password">
        <button>Login</button>
    </form>
    </div>
</body>
</html>